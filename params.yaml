# params.yaml
model:
  name: random_forest
  n_estimators: 200
  max_depth: 8
  min_samples_split: 2
  min_samples_leaf: 1
  random_state: 42
  n_jobs: -1

train:
  test_size: 0.2
  random_state: 42

evaluate:
  metrics:
    - r2_score
    - mean_absolute_error
    - root_mean_squared_error
